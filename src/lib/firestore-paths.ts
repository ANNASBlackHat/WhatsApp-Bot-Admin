/**
 * Re-export shim — canonical path builders live in `packages/schema`.
 * Import from `@/lib/firestore-paths` as before; do not add definitions here.
 */
export * from "../../packages/schema/src/firestore-paths";
