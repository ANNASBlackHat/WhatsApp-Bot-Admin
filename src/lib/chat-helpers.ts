/**
 * Re-export shim — canonical chat helpers live in `packages/schema`.
 * Import from `@/lib/chat-helpers` as before; do not add definitions here.
 */
export * from "../../packages/schema/src/chat-helpers";
