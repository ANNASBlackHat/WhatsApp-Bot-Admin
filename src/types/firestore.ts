/**
 * Re-export shim — canonical Firestore types live in `packages/schema`.
 * Import from `@/types/firestore` as before; do not add definitions here.
 */
export * from "../../packages/schema/src/firestore";
