{
  "name": "@app/schema",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Firestore schema (types + paths) for the WhatsApp bot admin monorepo",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "peerDependencies": {
    "firebase": "^12.16.0"
  }
}
