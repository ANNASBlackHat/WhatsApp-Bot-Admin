/**
 * Native entry point for `@app/data` (`@app/data/native`).
 *
 * Contract + `@react-native-firebase` implementation. Mobile-only — never
 * imported by the web app.
 */
export * from "./chat-source";
export * from "./native-source";
